# Copy to .env and fill in real API keys. Never commit .env.
#
# Each instance in configs/instances/instances.yaml names the env var that
# holds its API key via `api_key_env`. The defaults below match the mocked
# instances.yaml as shipped.

ARIZE_PROD_API_KEY=
ARIZE_NONPROD_API_KEY=
